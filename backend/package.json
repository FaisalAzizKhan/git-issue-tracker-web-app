{
  "name": "pod-web-backend",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "bun --watch --extension=ts run src/index.ts",
    "development": "cross-env NODE_ENV=development bun --watch --extension=ts run src/index.ts",
    "production": "cross-env NODE_ENV=production bun --watch --extension=ts run src/index.ts",
    "stage": "cross-env NODE_ENV=stage bun --watch --extension=ts run src/index.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.804.0",
    "@elysiajs/cors": "^1.3.0",
    "@elysiajs/jwt": "^1.3.0",
    "@elysiajs/swagger": "^1.3.0",
    "@prisma/client": "^6.7.0",
    "@types/jsonwebtoken": "^9.0.9",
    "cross-env": "^7.0.3",
    "dotenv": "^16.5.0",
    "elysia": "latest",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.3",
    "prisma": "^6.7.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "bun-types": "^1.3.10",
    "dotenv-cli": "^8.0.0"
  },
  "module": "src/index.js",
  "type": "module",
  "private": true,
  "peerDependencies": {
    "typescript": "^5"
  }
}
